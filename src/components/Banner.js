/**
 * Компонент "Рекламный горизонтальный баннер"
 * Принимает props:
 * - изображение баннера
 * - ссылка на сайт рекламодателя
 */
 export default function Banner({ image, link }) {
    return (
      <div>
        <img src="" alt="image shop"></img>
        <a href="#0">Link on shop</a>
      </div>
    );
  }